const createNewUser=  (req,res)=>{
    //check if the values are valid
    //go to DB and save the user
    //send a message to the view like user was added you can login now
}
const getAllUser=(req,res)=>{

    // go to DB and get all users 
    //send the data to the view to show them to the client 
}

module.exports={
    createNewUser,
    getAllUser
}